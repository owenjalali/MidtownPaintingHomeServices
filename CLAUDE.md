# Claude Code Notes

Use [AGENTS.md](./AGENTS.md) as the primary project guidance file.

## Quick commands
- End-to-end local run: `npm run dev:full:trigger`
- Frontend + API only: `npm run dev:full`
- Trigger worker only: `npm run dev:trigger`
- Calendar tests: `npm run test:calendar`

Keep this file lightweight and avoid duplicating long docs.
