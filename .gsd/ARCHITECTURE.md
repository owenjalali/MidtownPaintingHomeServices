# Architecture

> Auto-generated by /map on 2026-02-04

## Overview

This is a single-page web application built with React and Vite. It currently follows a flat structure with source files in the root directory.

```
┌─────────────────────────────────────────┐
│              [index.html]               │
├─────────────────────────────────────────┤
│            [React Components]           │
│              (index.tsx)                │
└─────────────────────────────────────────┘
```

## Components

### Root Application
- **Purpose:** Entry point and main component structure
- **Location:** `./index.tsx`
- **Dependencies:** `react`, `react-dom`

## Data Flow

1. **Entry:** Browser loads `index.html`
2. **Boot:** `index.tsx` mounts React root
3. **Render:** Components render to DOM

## Integration Points

*No external API integrations or database connections detected in current scan.*

## Technical Debt

- [ ] **Flat Structure:** Source files are in the root directory rather than a `src/` folder.
- [ ] **Missing Tests:** No test framework configured or test files found.

## Conventions

**Structure:** Currently flat; likely needs reorganization into `src/` as project grows.
**Naming:** Standard React/TypeScript conventions (`.tsx`, `camelCase`).
